export const loader = (state) => {
    return state.loader;
}

export const successAlert = (state) => {
    return state.successAlert;
}

export const message = (state) => {
    return state.message;
}