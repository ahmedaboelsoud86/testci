export const SET_DOCTORS = (state, doctors) => {
    state.test = doctors;
}

export const SET_DOCTOR = (state, doctor) => {
    state.doctoredite = doctor;
}

export const SET_TOTAL = (state, doctors) => {
    state.total = doctors;
}

export const SET_ERRORS = (state, errors) => {
    state.errors = errors;
}
export const REMOVE_ERRORS = (state) => {
    state.errors = "";
}
export const SET_CAT = (state, cats) => {
    state.cats = cats;
}
export const SET_CODE = (state, code) => {
    state.code = code;
}










